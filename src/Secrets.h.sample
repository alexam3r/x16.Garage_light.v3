#pragma once

// =============================================================================
// Шаблон секретов. Скопируйте этот файл в src/Secrets.h и заполните реальные
// значения. Secrets.h исключён из git (.gitignore) — не коммитить креды.
// =============================================================================

// Wi-Fi
#define WIFI_SSID           "YOUR_WIFI_SSID"
#define WIFI_PASSWORD       "YOUR_WIFI_PASSWORD"

// MQTT-брокер
#define MQTT_HOST           "192.168.1.100"   // IP или hostname брокера
#define MQTT_USER           ""                // пусто = без auth
#define MQTT_PASSWORD       ""                // пусто = без auth

// Уникальный ID клиента (меняйте, если устройств больше одного)
#define MQTT_CLIENT_ID      "garage-light-esp8266"
